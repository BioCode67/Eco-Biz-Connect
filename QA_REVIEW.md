# Eco-Biz Connect — 출시 가능성 자가진단 (QA Review)

갱신일: 2026-06-14 · 기준: "지금 이대로 실제 상용 서비스로 출시 가능한가?"

Apple 스타일 디자인 시스템 + 라이트/다크 모드, 웹↔앱 완전 기능 파리티, ESG 임팩트 신규 기능까지
적용한 뒤의 상태를 재점검한다. (심각도 🔴 높음 · 🟠 중간 · 🟡 낮음)

## 전수 검증 결과 (2026-06-14 야간 스윕)

| 검증 항목 | 방법 | 결과 |
|------|------|------|
| 프런트↔백엔드 API 연결 | 웹·앱의 모든 `api()` 경로 vs 백엔드 라우트 대조 | 19개 호출 경로 전부 매칭 — **끊긴 연결 0** |
| 외부 연동 mock | 서비스 코드의 실제 네트워크 호출(requests/httpx/socket) 탐색 | **0건** — 5개(AI·Bank·Blockchain·KYC·Storage) 전부 self-contained mock |
| mock ↔ 흐름 연결 | 각 mock 호출 지점 추적 | ai_engine←파이프라인, bank←대출·결제, blockchain←발행·배당·구매·앵커, kyc←인증, storage←업로드 모두 wiring 확인 |
| 엔드포인트 런타임 | 4개 역할로 27개 엔드포인트 E2E 호출 | **전부 정상**(GET 200·구매 201·발행 201·정지/복원 200·웹훅 200·PDF 200) |
| 입력 검증 | 과다구매·0·음수·미존재·약한비번·중복·필수누락·잘못된이메일 | 8/8 정확(409·422·404 적절 반환) |
| 접근제어 | 역할 위반 호출 | 소상공인→마켓/관리자 403, 무토큰 401 |
| 단위 테스트 | `pytest` | **64개 통과** |
| 웹 빌드 | `next build` | 11페이지 0오류 |
| 앱 번들 | `tsc --noEmit` + `expo export` | 통과(710 모듈) |
| 웹 시각 QA | 프리뷰 스크린샷(라이트+다크) | 5개 화면 + 신규 기능(ESG 추이·탄소 임팩트·플랫폼 임팩트) 정상 |

## 이전 라운드에서 해소된 약점
- 🔴 네트워크 오류 재시도 UX(ErrorBanner) · 🔴 스크롤 모션 폴백 · 🟠 반응형 테이블/모바일 드로어
- 🟠 접근성(label 연결·세그먼트 role·모달 aria·토스트 aria-live)
- ✅ **UC6 상품 비교**(웹·앱 모두 — 최대 3개·항목별 최적값 ★)
- ✅ **UC13 시스템 알림 피드**(웹·앱 모두 — 서브시스템 경고+감사 이벤트)
- ✅ 배포 자동 시드(`--if-empty`) — 공개 링크 빈 DB 문제 해소

## 남은 한계 (기록 — 평가 핵심 플로우 외 / 구조적)
- 🟡 **앱 다크 모드 미적용**: RN `StyleSheet.create`가 모듈 스코프에서 정적 `colors`를 참조해
  다크 지원 시 전 파일 스타일 리팩터 필요 + 시뮬레이터 부재로 시각 검증 불가 → 위험 대비 가치 낮아 보류(웹은 완비).
- 🟠 토큰 localStorage/secure-store 저장(무료 호스팅 SPA 구조상 한계, 설계서는 httpOnly 권장).
- 🟡 Render 무료 백엔드 콜드스타트 30~60초 · PostgreSQL 무료 90일 만료(DEPLOYMENT.md 안내).
- 외부연동(블록체인/AI/Bank/KYC)은 설계대로 mock 유지(Phase 5는 실계정·키 필요).

# Eco-Biz Connect — 출시 가능성 자가진단 (QA Review, Apple 재디자인 후)

갱신일: 2026-06-11 · 기준: "지금 이대로 실제 상용 서비스로 출시 가능한가?"

이전 라운드에서 반응형·JWT 갱신·기본 접근성·에러 메시징을 개선했고, 이번엔 **Apple 스타일
디자인 시스템 + 전 화면 재구축** 이후 남은 약점을 재점검한다. (심각도 🔴 높음 · 🟠 중간 · 🟡 낮음)

| 영역 | 약점 | 심각도 | 조치 |
|------|------|--------|------|
| 에러 처리 | 초기 데이터 로드 실패(백엔드 다운/콜드스타트 타임아웃)를 `.catch(()=>[])`로 삼켜 **빈 상태로만 표시** → 사용자가 "데이터 없음"과 "연결 실패"를 구분 못 함 | 🔴 | **이번 수정**: 네트워크 오류(ApiError status 0) 시 재시도 배너 표시 |
| 접근성 | 로그인 폼 `<label>`이 입력과 `htmlFor/id`로 명시 연결되지 않음 / 세그먼트 컨트롤에 그룹 역할 라벨 없음 | 🟠 | **이번 수정**: id 연결 + aria |
| 모션 | 스크롤 등장이 관찰자 미작동 시 안 보일 위험 | 🔴 | **해결됨**(직전 단계): 가시성 폴백(즉시표시+타임아웃) |
| 반응형 | 넓은 테이블이 폰에서 레이아웃 깨짐 | 🟠 | **해결됨**(직전 단계): 카드 내 가로 스크롤 |
| 기능 완전성 | 이메일 인증·UC6 상품 비교·UC13 알림 피드 미구현 | 🟡 | 평가 핵심 흐름 아님 — 한계로 기록 |
| 검증 | 관리자 STO 발행 폼 숫자 음수/0 프론트 검증 약함(백엔드는 검증) | 🟡 | 백엔드 차단 + 토스트로 안전, 우선순위 낮음 |
| 보안 | 토큰 localStorage 저장(XSS 취약, 설계서는 httpOnly 권장) | 🟠 | 무료 호스팅 SPA 구조상 한계 — 기록 |
| 콜드스타트 | Render 무료 백엔드 첫 요청 30~60초 | 🟡 | DEPLOYMENT.md 안내 + 로딩 스켈레톤으로 완충 |

## 이번 라운드 수정(영향 큰 것 우선)
1. 🔴 **네트워크 오류 재시도 UX**: 대시보드 초기 로드가 네트워크 오류로 실패하면 빈 상태 대신
   "연결 실패 · 다시 시도" 배너를 보여준다. (정상 404/빈 데이터는 그대로 빈 상태 유지)
2. 🟠 **접근성**: 로그인 입력 `htmlFor/id` 연결, 세그먼트 컨트롤 `role=group` + `aria-label`.

## 남은 한계(기록)
- 이메일 인증/상품 비교/실시간 알림 피드: 평가 핵심 플로우 외 — mock 또는 미구현.
- 토큰 저장 방식(localStorage), Render/Vercel 무료 플랜 콜드스타트·90일 DB 만료.
- 외부연동(블록체인/AI/Bank)은 설계대로 mock 유지(Phase 5는 실계정·키 필요).
